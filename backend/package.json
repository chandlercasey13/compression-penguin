{
  "name": "compression-penguin-backend",
  "version": "1.0.0",
  "description": "Lambda-driven image compression API",
  "type": "module",
  "scripts": {
    "test": "node --test src/**/*.test.js",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-lambda": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "sharp": "^0.33.5",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
